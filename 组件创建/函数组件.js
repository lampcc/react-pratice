import React from 'react';

// 组件创建
/**
 * 函数创建组件
 * 1. 定义一个函数（箭头函数）
 * 2. 函数体内返回元素（jsx）
 * 3. 使用函数名作为标签名
 *   注意：函数名要以大驼峰形式命名
 */

const Hello = () => {


  return (
    <h1>函数组件</h1>
  )
}

export default Hello